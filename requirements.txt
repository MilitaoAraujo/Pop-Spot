# Pop-Spot — dependências Python
#
# Instale com: pip install -r requirements.txt
#
# Pacotes de sistema: use bash install.sh (Pop!_OS / Ubuntu / Debian).

requests>=2.28.0
numpy>=1.23.0
PySide6>=6.6.0
